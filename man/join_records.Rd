% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/join_records.R
\name{join_records}
\alias{join_records}
\title{Join Records Using Linkage Results}
\usage{
join_records(
  links,
  A,
  B,
  all = FALSE,
  all_A = all,
  all_B = all,
  suffixes = c(".a", ".b"),
  keep_from_links = FALSE
)
}
\arguments{
\item{links}{A linkage result from \code{\link[=mec]{mec()}}, \code{\link[=predict.rec_lin_model]{predict.rec_lin_model()}},
\code{\link[=mec_blocking]{mec_blocking()}}, or a \code{data.frame}/\code{data.table} with columns \code{a} and \code{b}.}

\item{A}{A \code{data.frame} or \code{data.table}.}

\item{B}{A \code{data.frame} or \code{data.table}.}

\item{all}{Logical indicating whether to include unmatched records from both
datasets.}

\item{all_A}{Logical indicating whether to include unmatched records from \code{A}.}

\item{all_B}{Logical indicating whether to include unmatched records from \code{B}.}

\item{suffixes}{A character vector of length two used to distinguish columns
from \code{A} and \code{B} when their names conflict with each other or with linkage
columns.}

\item{keep_from_links}{Logical or character vector. If \code{FALSE}, only columns
\code{a} and \code{b} are kept from the linkage table. If \code{TRUE}, all non-index linkage
columns are kept. If a character vector, only the selected non-index linkage
columns are kept.}
}
\value{
Returns a \code{data.table} containing:\cr
\itemize{
\item \code{a} -- row indices of records from \code{A},
\item \code{b} -- row indices of records from \code{B},
\item columns selected from \code{links} -- linkage metadata kept according to
\code{keep_from_links}, if requested,
\item columns from \code{A} -- values of records from \code{A}, with \code{suffixes[1]}
added when needed,
\item columns from \code{B} -- values of records from \code{B}, with \code{suffixes[2]}
added when needed.
}
}
\description{
Joins two datasets using row-index pairs returned by record linkage.
}
\note{
The function follows the general design of \link[reclin2:link]{link()}, adjusted
to linkage results used in \code{automatedRecLin}.
}
\examples{
A <- data.frame(name = c("James", "Emma"), age = c(30, 28))
B <- data.frame(name = c("James", "Emily"), city = c("Boston", "Denver"))
links <- data.frame(a = 1, b = 1, ratio = 10)
join_records(links, A, B)
}
\author{
Adam Struzik
}
